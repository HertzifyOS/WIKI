## Downloading Source
To start building HertzifyOS, you need to synchronize our manifest with your local environment (Arch Linux is recommended).

### Initialization
First, install the `repo` tool and initialize the HertzifyOS manifest:
```bash
repo init -u [https://github.com/HertzifyOS/android.git](https://github.com/HertzifyOS/android.git) -b 16